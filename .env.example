# Option A (recommended on Railway): reference MySQL service outputs
# In Railway Variables Raw Editor, you can add direct references instead of raw values.
# Use the "Add Reference" UI if available.
MYSQLHOST=
MYSQLPORT=
MYSQLDATABASE=
MYSQLUSER=
MYSQLPASSWORD=

# Option B (manual values)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=crud_app
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4
